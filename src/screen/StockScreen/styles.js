import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  mainTitle: {
    marginTop: 50,
    fontSize: 22,
    fontWeight: "bold",
    textAlign: "center",
  },
  dataContainer: { margin: 5, borderRadius: 10, borderWidth: 0.5 },
  contTitle: { fontSize: 18, fontWeight: "bold", marginLeft: 10 },
});
